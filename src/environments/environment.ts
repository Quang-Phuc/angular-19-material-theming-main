export const environment = {
  /**
   * Đánh dấu đây KHÔNG PHẢI là môi trường production.
   */
  production: false,

  /**
   * Link API máy chủ local của bạn.
   * (Ví dụ: khi bạn chạy API ở máy của mình)
   */
  apiUrl: 'http://localhost:3000/v1'
};
